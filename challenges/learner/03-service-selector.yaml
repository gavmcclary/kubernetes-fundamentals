apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-demo
  template:
    metadata:
      labels:
        app: service-demo
    spec:
      containers:
        - name: app
          image: nginx:1.27
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: service-demo
spec:
  selector:
    app: service-demoo
  ports:
    - port: 80
      targetPort: 80
